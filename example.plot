// Classic function plotting
sin(x) #color="cyan"
x ** 2 + x - 1 "Quadratic \"function\""

// Custom variables
a = 1 / x
b = a ** 2

// Custom functions
f(x,y) = sin(x) * cos(y)
f(x, 2*x) #linestyle="dashed"

// Polar plots
r = theta

g = pi ** 2

// Parametric plots
// (t + sin(t), 1 - cos(t))[t,-10,10]
